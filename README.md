# Secure-3-Tier-VPC-on-AWS
This project implements a production-style 3-tier VPC (public, private app, private DB) across 2 AZs using AWS Console only — no SSH keys required (SSM used for private access).
